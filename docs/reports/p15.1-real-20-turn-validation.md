# P-15.1 Real 20-Turn Validation Report

> Source of truth for this report:
>
> - `artifacts/rp-mvp-v1/run-summary.json` (raw run summary, machine-checked)
> - `artifacts/rp-mvp-v1/narrative-review-extract.md` (turn-by-turn narrative)
> - `artifacts/rp-mvp-v1/restart-evidence/` (turn 10 server restart evidence)
> - `artifacts/rp-mvp-v1/turns/turn-*.json` (per-turn artifacts)
> - `artifacts/rp-mvp-v1/agent-sessions/agent-sessions.json` (file session store)
> - `artifacts/rp-mvp-v1/workflow-memory/workflow-memories.json` (memory store)
>
> All numbers in this report are derived from those local artifacts. No estimates.

## 1. Frozen Baseline

```text
Branch:        master
HEAD:          09d53ac (perf(rp): trim critic and curator production context)
Tag:           phase-rp-playable-mvp-v1-stable
Session ID:    rp-mvp-v1-mqhz48xd
Provider:      deepseek
Model:         deepseek-v4-flash
Workflow:      rp-unified-stateful-production-v1
```

## 2. Run Configuration

- Real Provider: `RP_PROVIDER=deepseek`, `RP_MODEL=deepseek-v4-flash`, `DEEPSEEK_API_KEY` (read from local env, never committed).
- Persistent stores: `AGENT_SESSION_STORE=file`, `AGENT_SESSION_DIR=./artifacts/rp-mvp-v1/agent-sessions`, `WORKFLOW_MEMORY_STORE=file`, `WORKFLOW_MEMORY_DIR=./artifacts/rp-mvp-v1/workflow-memory`.
- Driver: `scripts/run-rp-mvp-v1.mjs`, gated by `RUN_REAL_RP_MVP_V1=1`.
- Sequence: 20 sequential `POST /api/rp` calls. Server is restarted after turn 10 with the same `sessionId`.

## 3. Headline Metrics (from `run-summary.json`)

| Metric                | Value           | Source field                                 |
| --------------------- | --------------- | -------------------------------------------- |
| Total turns           | 20              | `totalTurns`                                 |
| HTTP 200              | 20              | `httpOk`                                     |
| Accepted turns        | 20              | `acceptedTurns`                              |
| Exhausted turns       | 0               | `exhaustedTurns`                             |
| First-pass accepted   | 19              | derived (`acceptedTurns - revisionTurns`)    |
| Revision accepted     | 1               | `revisionTurns`                              |
| LLM calls             | 62              | `llmCalls`                                   |
| Writer calls          | 21              | `writerCalls`                                |
| Critic calls          | 21              | `criticCalls`                                |
| Curator calls         | 20              | `curatorCalls`                               |
| Input tokens          | 79,807          | `inputTokens`                                |
| Output tokens         | 44,824          | `outputTokens`                               |
| Total tokens          | 124,631         | `totalTokens`                                |
| Usage unavailable     | 0               | `usageUnavailable`                           |
| Session conflicts     | 0               | per-turn `sessionCommit.conflict` aggregated |
| Total latency (ms)    | 593,225         | `totalLatencyMs`                             |
| Average latency (ms)  | 29,661          | `averageLatencyMs`                           |
| P50 latency (ms)      | 26,796          | `p50LatencyMs`                               |
| P95 latency (ms)      | 39,826          | `p95LatencyMs`                               |
| Max turn tokens       | 11,837 (turn 8) | `maxTurnTokens`                              |
| Max turn latency (ms) | 60,575 (turn 8) | `maxTurnLatencyMs`                           |

## 4. Per-Turn Snapshot

The full per-turn data is in `artifacts/rp-mvp-v1/run-summary.json#turns[]` and `artifacts/rp-mvp-v1/turns/turn-*.json`. Each turn was:

- HTTP 200
- `accepted: true`
- `exhausted: false`
- `sessionCommit.conflict: false`
- `memoryCommit.attempted: true`, `written: 1`, `deduplicated: false`
- LLM calls = 3 (writer + critic + curator)
- `usageUnavailable: 0`

The only exception is turn 8, where `writerAttempts: 2`, `criticAttempts: 2`, `revisionApplied: true`, `llmCalls: 5` — the one revision turn, and also the largest turn in tokens (11,837) and latency (60,575 ms).

## 5. Restart Continuity (Turn 10 → Turn 11)

- Evidence files:
  - `artifacts/rp-mvp-v1/restart-evidence/restart-after-turn-10.json`
  - `artifacts/rp-mvp-v1/restart-evidence/server-initial.log`
  - `artifacts/rp-mvp-v1/restart-evidence/server-after-turn-10.log`
- `restart-after-turn-10.json` records `stoppedAfterTurn: 10` and the stop timestamp.
- Turn 10 player input: "你还记得第一轮我放到你面前的东西是什么吗？" → Mio answers "钥匙，铜绿色的，柄上缠着褪了色的红线" (recalling turn 1's key).
- Turn 11 player input: "继续" → Mio continues the same thread, identifies the key, and references the "末班车" — the `sessionId` remained `rp-mvp-v1-mqhz48xd` across the server restart.
- Conclusion: server restart does not break session continuity. The file session store survives process exit.

## 6. Turn 8 Revision (the only revision)

- `writerAttempts: 2`, `criticAttempts: 2`, `revisionApplied: true`, `llmCalls: 5`, `tokens: 11,837`.
- Player input: "我停在门口，没有进去，请她说明她自己的选择。"
- Revision produces a passage where Mio explains "我选择留在这里…" and frames the key as a lock waiting to be opened — consistent with the player's agency request, not a player-action hijack.
- Single revision turn out of 20. No exhausted turn.

## 7. Warning: Turn 14 Narrative Repeat

- Turn 14 player input: "请区分钥匙这件旧事和刚才脚步声这件新事。"
- Output: identical to turn 13 ("广播里的旋律忽然变了调…"). The user's explicit request to distinguish old event (key) from new event (footsteps) is not addressed.
- The 5-3 commit relaxed critic soft-thresholds; this single soft issue is the only artifact of that change in the 20-turn run. Not a blocker for freezing.
- See `docs/project-state-through-p15.1.md` § 8 (W-1) for the documented remediation rule.

## 8. Session and Memory Stores (Post-Run)

- `artifacts/rp-mvp-v1/agent-sessions/agent-sessions.json`:
  - Top-level keys: `[version, sessions, commitDedup]`.
  - Contains 20 entries, one per turn, with CJK content for both `userInput` and `assistantOutput`.
  - `commitDedup` enabled; per-turn `sessionCommit.committed: true`, `deduplicated: false`, `conflict: false`.
- `artifacts/rp-mvp-v1/workflow-memory/workflow-memories.json`:
  - Top-level keys: `[version, records, dedup]`.
  - 20 accepted memory records (one per turn), no dedup hits.

## 9. UTF-8 / Encoding Check (from freeze review)

- Node.js `JSON.parse` of both stores: OK.
- Replacement-character (`U+FFFD`) count: 0 (Node and PowerShell both report 0).
- No real data corruption. The "mojibake" observed earlier was a terminal display issue, not a file content issue.

## 10. Conclusion

All headline metrics are real observations from the artifacts listed in the front matter, not estimates. The run is fully consistent with `phase-rp-playable-mvp-v1-stable` at `09d53ac`. The single turn-14 narrative repeat is documented as a non-blocking warning with a workflow-layer remediation path. RP Playable MVP V1 is validation-complete.
