# P-15.1 Independent Freeze Review Report

> Source of truth: this report was authored at the moment of the freeze review.
> The original `.txt` form has been preserved in the operator's local working
> directory (outside the repo, untracked). The Markdown form below is the
> long-lived source of truth inside the repo.
>
> Verified against:
>
> - Git history (`git rev-parse --short HEAD` at review time: `09d53ac`)
> - `git tag --points-at HEAD` (`phase-rp-playable-mvp-v1-stable`)
> - `artifacts/rp-mvp-v1/run-summary.json`
> - `artifacts/rp-mvp-v1/restart-evidence/`
> - `artifacts/rp-mvp-v1/agent-sessions/agent-sessions.json`
> - `artifacts/rp-mvp-v1/workflow-memory/workflow-memories.json`
> - `docs/rp-playable-mvp-v1.md`
> - `data/workflows/rp-unified-stateful-production-v1.json`

## 1. Branch / HEAD Verification

```text
Branch:    master (fast-forwarded to feat/rp-playable-mvp-v1 prior to review)
HEAD:      09d53ac perf(rp): trim critic and curator production context
Worktree:  clean, `git diff --check` reports no whitespace errors
```

The review worked against the frozen state. No reset, rebase, checkout, or
tag-move was performed.

## 2. Independent Git Confirmation

- HEAD = `09d53ac`. The 5-3 commit changes 15 files, +2728/-14. Verified.
- The 5-3 theme matches the diff:
  - Critic profile gains hard/soft error judgement and evidence requirement.
  - `minStyleAndFormat` threshold `0.65 → 0.5`.
  - New `hasHardReviseJustification` covers evidence-less `revise` calls.
  - `revisionInstruction` 2000 → 1000 chars with issue count cap.
  - Workflow `critic1` / `critic2` / `curator` context switched from `ctxMerge2`
    to `wbMd` (worldbook only).
  - New `critic2Instruction` builder node.
- `artifacts/` is git-ignored: `git ls-files artifacts/rp-mvp-v1` is empty. No
  `.env` / secrets / credentials / local tool dirs are tracked
  (`.env.example` is the empty-value template only).
- `env.ts` adds an `OPENCODE_GO_API_KEY` fallback for runtime provider
  compatibility. It does not touch Writer prompt text / session idempotency /
  memory `operationId` / Checkpoint / Official API contracts. The new
  `criticInstructionBuilder` is additive, no contract change.

## 3. Manual 20-Turn Review (no LLM re-judgement)

| Turn | Quality             | Continuity                                   | Player Agency                                  | Knowledge Boundary        | Issue                    |
| ---- | ------------------- | -------------------------------------------- | ---------------------------------------------- | ------------------------- | ------------------------ |
| 1    | pass                | —                                            | ✓ player places key                            | ✓                         | —                        |
| 2    | pass                | ✓ key continues                              | ✓                                              | ✓                         | —                        |
| 3    | pass                | ✓ red thread / station master's room         | ✓                                              | ✓                         | —                        |
| 4    | pass                | ✓ old station ticket gate                    | ✓                                              | ✓                         | —                        |
| 5    | pass                | ✓ waiting hall / broadcast                   | ✓ only states intent                           | ✓                         | —                        |
| 6    | pass                | ✓ melody continues                           | ✓                                              | ✓                         | —                        |
| 7    | pass                | ✓                                            | ✓ does not know player's dream (boundary kept) | ✓ explicit "I don't know" | —                        |
| 8    | pass (revision)     | ✓ iron gate / key                            | ✓ answers "her choice"                         | ✓                         | revision reasonable      |
| 9    | pass                | ✓ coat / broadcast                           | ✓                                              | ✓                         | —                        |
| 10   | pass                | ✓ memory of key                              | ✓                                              | ✓                         | —                        |
| 11   | pass (post-restart) | ✓ same session continues                     | ✓                                              | ✓                         | natural bridge           |
| 12   | pass                | ✓ waiting hall door                          | ✓                                              | ✓                         | —                        |
| 13   | pass                | ✓ footsteps as new event                     | ✓                                              | ✓                         | —                        |
| 14   | warning             | ✗ identical to turn 13                       | ✗ "distinguish old vs new" ignored             | —                         | repeat + ignored request |
| 15   | pass                | ✓ note / boundary                            | ✓ does not read the note (boundary kept)       | ✓ explicit "I don't know" | —                        |
| 16   | pass                | ✓ opens door / waiting hall / extra umbrella | ✓                                              | ✓                         | —                        |
| 17   | pass                | ✓ two umbrellas                              | ✓ does not push for confession                 | ✓                         | —                        |
| 18   | pass                | ✓ position / rain intensity                  | ✓                                              | ✓                         | —                        |
| 19   | pass                | ✓ memory of key / red thread                 | ✓                                              | ✓                         | —                        |
| 20   | pass                | ✓ broadcast / umbrella forward hook          | ✓ gives player choice                          | ✓                         | —                        |

### Issue Tiers

- blocker: 0
- warning: 1 — turn 14 narrative is identical to turn 13 ("广播里的旋律忽然变了调…侧耳倾听"). The player's "请区分钥匙旧事与脚步声新事" explicit request was ignored. This is a real-Provider single soft-output occurrence after the 5-3 soft-threshold relaxation. Critic accepted it (`minStyleAndFormat` lowered to 0.5, evidence-less `revise` covered). It is the expected side-effect of the 5-3 trimming, isolated 1/20, and is **not** a data corruption / contract violation / format leak. Per the rule "do not block on style", it is logged as a freezable warning.
- note: 1 — `agent-sessions.json#sessions[].tokenUsage.input/output` are small integers (e.g. 45/40) that do not equal Provider usage. They are session-store internal estimates, not Provider observability. Non-blocking.

## 4. Turn 8 Revision Evaluation

`revisionApplied: true`, `writerAttempts: 2`, `criticAttempts: 2`, `llmCalls: 5`,
`tokens: 11,837` (largest turn in the run). The revised passage answers the
player's "请她说明她自己的选择" — Mio explains "留在那里" and frames the key
as a transition. Player agency is preserved, no player-action hijack.
Revision is reasonable. Only revision turn in the run.

## 5. Turn 10 / 11 Continuity Evaluation

- `restart-evidence/restart-after-turn-10.json` records `stoppedAfterTurn: 10`, `at: 2026-06-17T11:19:40Z`.
- Turn 10 player: "你还记得第一轮我放到你面前的东西是什么吗？" → Mio answers key (铜绿, 红线).
- Turn 11 player: "继续" → Mio continues the same thread (recognizes key, 末班车).
- `sessionId` is `rp-mvp-v1-mqhz48xd` throughout. Restart bridge is natural; same-session continuity works.

## 6. Session / Memory UTF-8 Check

- Node.js UTF-8 + `JSON.parse`: OK. Top-level keys `[version, sessions, commitDedup]`, 387 CJK tokens.
- `U+FFFD` count: 0 (Node and PowerShell agree).
- PowerShell `Get-Content -Encoding UTF8 + ConvertFrom-Json`: succeeds, `replacement_chars: 0`.
- Session store contains 20 turns of Chinese; `userInput` and `assistantOutput` parse correctly.
- Conclusion: the earlier "mojibake" was a terminal display issue, not data corruption. Non-blocking. No file modified.

## 7. Artifact Recomputation (20-turn JSON + `run-summary.json`)

| Metric              | Declared | Recomputed                | Match |
| ------------------- | -------- | ------------------------- | ----- |
| `totalTurns`        | 20       | 20                        | ✓     |
| `httpOk`            | 20       | 20                        | ✓     |
| `acceptedTurns`     | 20       | 20                        | ✓     |
| `exhaustedTurns`    | 0        | 0                         | ✓     |
| `firstPassAccepted` | 19       | 19                        | ✓     |
| `revisionAccepted`  | 1        | 1 (turn 8 only)           | ✓     |
| `llmCalls`          | 62       | 62                        | ✓     |
| `inputTokens`       | 79,807   | 79,807                    | ✓     |
| `outputTokens`      | 44,824   | 44,824                    | ✓     |
| `totalTokens`       | 124,631  | 124,631                   | ✓     |
| `usageUnavailable`  | 0        | 0                         | ✓     |
| `sessionConflict`   | 0        | 0 (all `conflict: false`) | ✓     |

`run-summary.json` uses the field name `revisionTurns: 1` (semantically
equivalent to the declared `revisionAccepted: 1`); not a conflict.
`workflow-memory` store `{version, records, dedup}` JSON is intact.
Turn 10 restart → turn 11 same session is corroborated by artifacts; no
guess-work, no fabrication.

## 8. Non-Real Quality Gates

- `npm run format:check`: All files use Prettier code style ✓
- `npm run typecheck` (`tsc -b`): exit 0 ✓
- `npm run lint` (`eslint .`): exit 0 ✓
- `npm test`: `agent-runtime` 18 files / 322 tests, `server` 106 passed / 8 skipped, `web` 18 files / 107 tests, all other workspaces green ✓
- `npm --workspace @awp/agent-runtime test`: 322 passed ✓
- `npm --workspace @awp/server test`: 106 passed ✓
- `npm --workspace @awp/web test`: 107 passed ✓
- `npm run build`: exit 0 ✓
- `npm run verify` rerun: exit 0 ✓
- `git diff --check`: no whitespace errors ✓; `git status --short`: clean ✓
- No real Provider invoked ✓

## 9. Secret Scan

- Patterns scanned: `sk-` / `api_key` / `authorization` / `bearer` / `DEEPSEEK_API_KEY=` / `OPENCODE_*API_KEY=` / provider request-response traces / prompt or system prompt or critic JSON content.
- All hits in tracked files are template placeholders (empty `.env.example`, demo comment `sk-...`, documentation examples). No real keys.
- No `.env` is tracked. Artifacts are not in Git.
- No real keys found ✓.

## 10. Master Merge Method

At review time `master` and `feat/rp-playable-mvp-v1` were already at
`09d53ac` (fast-forward was a pre-existing fact). The review did not perform
any merge or rewrite; it only attached the tag on top of this state. No
production logic changed. Real 20-turn was not rerun. P-15.2 was not
started. No per-role token telemetry was added. No Gate / Prompt changed.

## 11. Final State

```text
master HEAD:           09d53ac (perf(rp): trim critic and curator production context)
Tag:                   phase-rp-playable-mvp-v1-stable
                       (annotated tag object, dereferences to commit 09d53ac)
Git status:            clean
Current branch:        master
```

## 12. Outstanding Warnings

1. **W-1 (turn-14 repeat, non-blocking)** — turn 14 narrative is identical to turn 13 (broadcast melody sudden change → turn to listen); the player's "distinguish old vs new" request was ignored. This is a single soft-output artifact of the post-5-3 critic/curator context-trim relaxation. Per project policy (`rp-workflow-comfyui-constraint` memory), any future mitigation must be expressed through workflow / node / parameter composition, not hard-coded into the program; any code change must be confirmed with the user first.
2. **N-1 (session `tokenUsage` estimate, non-blocking)** — session-store `tokenUsage` is a small-integer estimate, not Provider observability. Internal to the session store, does not affect billing or observability contracts.

## 13. Freeze Verdict

**RP Playable MVP V1: frozen.**

All freeze conditions met:

- Git candidate is real ✓
- 20-turn artifacts match the report ✓
- Manual review has no blocker (1 warning) ✓
- Session files have no real encoding damage ✓
- Turn 8 revision is reasonable ✓
- Turn 10/11 restart continuity works ✓
- All non-real quality gates pass ✓
- Secret scan has no hits ✓
- Worktree is clean ✓

The tag `phase-rp-playable-mvp-v1-stable` is attached and points at `09d53ac`.
